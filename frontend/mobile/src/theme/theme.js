import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#1976d2',
    accent: '#f50057',
    background: '#f5f5f5',
    surface: '#ffffff',
    text: '#212121',
    error: '#B00020',
  },
};

export default theme;
